ABI=aapcs-linux
ARCH=armv6j
MODE=arm
FPU=vfp
FLOAT=hard
TUNE=arm1176jzf-s

export CFLAGS="${CFLAGS} -O2"

#BUILD_DIR="${PKGMK_BASEDIR}/build"
TOOLCHAIN_DIR="/toolsx"
#export PATH=/tools/bin:${PATH}

CLFS_HOST=x86_64-cross-linux-gnu
CLFS_TARGET=arm-unknown-linux-gnueabi
CLFS_DIR="${PKGMK_BASEDIR}/clfs"
CLFS_TARGET_TOKEN="linux-musl"

NR_JOBS=6


KERNEL="linux-rpi"
